# ============================================================
# 🛡️ FIX F-17: Pinned dependencies (supply chain protection)
# ============================================================
# All versions pinned to known-good combinations.
# To regenerate from requirements.in:
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file=requirements.txt requirements.in
#
# To upgrade a single package:
#   pip-compile --upgrade-package streamlit
# ============================================================

# ── Core ──
streamlit==1.39.0
python-dotenv==1.0.1
requests==2.32.3
urllib3==2.2.3

# ── Document parsing ──
PyPDF2==3.0.1
pdf2image==1.17.0
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.0
beautifulsoup4==4.12.3
ebooklib==0.18
striprtf==0.0.26

# ── Images / OCR ──
Pillow==10.4.0
pytesseract==0.3.13
easyocr==1.7.1

# ── ML / Embeddings / Vector DB ──
torch==2.4.1
sentence-transformers==3.1.1
faiss-cpu==1.8.0.post1
scikit-learn==1.5.2
numpy==1.26.4

# ── API / Load testing ──
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
python-multipart==0.0.10
locust==2.31.0
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0

# ── Security / Verification ──
python-magic==0.4.27
certifi==2024.8.30

# ── Observability (optional) ──
sentry-sdk==2.14.0
redis==5.0.8

# ── PyMuPDF (better PDF fallback if PyPDF2 fails) ──
PyMuPDF==1.24.10
