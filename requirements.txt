 #==============================================================
# Smart Document Q&A - Requirements (v3.0.5 - ASCII Safe)
# ==============================================================
# v3.0.5 FIX: Removed all non-ASCII characters (emojis, arrows, etc.)
# Windows pip uses cp1252 encoding which can't read unicode chars.
# ==============================================================

# =========================
# Core
# =========================
streamlit>=1.40.0
python-dotenv>=1.0.1
requests>=2.32.0
urllib3>=2.2.0
certifi>=2024.8.30

# =========================
# Document Processing
# =========================
PyPDF2>=3.0.1
PyMuPDF>=1.24.0
pdf2image>=1.17.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0
beautifulsoup4>=4.12.0
ebooklib>=0.18
striprtf>=0.0.26

# =========================
# OCR (Tesseract - for images + scanned PDFs)
# =========================
Pillow>=10.0.0
pytesseract>=0.3.10

# =========================
# Machine Learning - CPU ONLY (no CUDA!)
# =========================
# CRITICAL: extra-index-url tells pip to use PyTorch CPU wheels
# This prevents CUDA packages (3GB+) from being installed.
--extra-index-url https://download.pytorch.org/whl/cpu

# CPU-only torch (no CUDA suffix, no CUDA packages)
torch>=2.0.0
torchvision>=0.15.0

# Local embeddings (works on CPU, no API key needed)
sentence-transformers>=2.2.0

# Vector store
faiss-cpu>=1.8.0

# numpy - no version pin (let sentence-transformers pick compatible version)
numpy>=1.24.0

# =========================
# Utilities
# =========================
python-magic>=0.4.27
redis>=5.0.0
sentry-sdk>=2.0.0